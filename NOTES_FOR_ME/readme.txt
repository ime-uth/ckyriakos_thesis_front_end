1) GIA TO HEATMAP/CLUSTER, MPORW NA XRHSIMOPOIHSW EITE TO GREEK_CITIES EITE TO /geojson/greece_gdf



Total request size (33030144 pixels) must be less than or equal to 26214400 pixels. # gia gif sto timelapse


2)Used the geemap.fishnet to split the user selected roi

3)Managed to create a FeatureCollection for the specified roi and got time series using
the getTimeSeriesByRegions function.

In case the elements created from that are more than 5000 gee can't do calculations so I have to split them and append them later.
So I shoudl Implement that now.